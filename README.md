## Quick Start:

To Install, run this in your CLI:

``` yarn ```

To run in localhost, run this in your CLI:

``` yarn dev ```

1. Navigate to `localhost:3000` in your browser of choice.
2. Click on the [Profiles]("localhost:3000/profile") hyperlink in the page.